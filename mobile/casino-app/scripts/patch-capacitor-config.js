/**
 * Ensures capacitor.config.ts is copied for CLI (Capacitor reads .ts via ts-node when present).
 * No-op placeholder for postinstall hook stability on Windows.
 */
console.log('[casino-app] Capacitor config ready (capacitor.config.ts)');
