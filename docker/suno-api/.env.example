# Suno session cookie — required for gcui-art/suno-api proxy.
# Get from browser: suno.com → DevTools → Application → Cookies → __client
SUNO_COOKIE=

# Optional: restrict proxy to localhost (default bind is 127.0.0.1:3000 in compose)
# BROWSER=chromium
# BROWSER_HEADLESS=1
