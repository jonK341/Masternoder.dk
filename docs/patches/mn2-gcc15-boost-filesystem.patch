--- a/src/init.cpp
+++ b/src/init.cpp
@@ -1034,7 +1034,8 @@ bool AppInitMain()
     std::string strDataDir = GetDataDir().string();
 #ifdef ENABLE_WALLET
     // Wallet file must be a plain filename without a directory
-    if (strWalletFile != boost::filesystem::basename(strWalletFile) + boost::filesystem::extension(strWalletFile))
+    boost::filesystem::path walletPath(strWalletFile);
+    if (strWalletFile != walletPath.stem().string() + walletPath.extension().string())
         return InitError(strprintf(_("Wallet %s resides outside data directory %s"), strWalletFile, strDataDir));
 #endif
     // Make sure only a single MasterNoder2 process is using the data directory.
