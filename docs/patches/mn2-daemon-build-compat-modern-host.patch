diff --git a/src/httpserver.cpp b/src/httpserver.cpp
index 0ddaa43..3423ad4 100755
--- a/src/httpserver.cpp
+++ b/src/httpserver.cpp
@@ -18,6 +18,7 @@
 #include <string.h>
 
 #include <sys/types.h>
+#include <deque>
 #include <sys/stat.h>
 #include <signal.h>
 #include <future>
diff --git a/src/init.cpp b/src/init.cpp
index ed800a5..55e11a8 100755
--- a/src/init.cpp
+++ b/src/init.cpp
@@ -60,6 +60,7 @@
 #include <boost/algorithm/string/predicate.hpp>
 #include <boost/algorithm/string/replace.hpp>
 #include <boost/filesystem.hpp>
+#include <boost/bind.hpp>
 #include <boost/interprocess/sync/file_lock.hpp>
 #include <boost/thread.hpp>
 #include <boost/foreach.hpp>
@@ -1034,7 +1035,8 @@ bool AppInit2()
     std::string strDataDir = GetDataDir().string();
 #ifdef ENABLE_WALLET
     // Wallet file must be a plain filename without a directory
-    if (strWalletFile != boost::filesystem::basename(strWalletFile) + boost::filesystem::extension(strWalletFile))
+    boost::filesystem::path walletPath(strWalletFile);
+    if (strWalletFile != walletPath.stem().string() + walletPath.extension().string())
         return InitError(strprintf(_("Wallet %s resides outside data directory %s"), strWalletFile, strDataDir));
 #endif
     // Make sure only a single MasterNoder2 process is using the data directory.
@@ -1044,7 +1046,7 @@ bool AppInit2()
     static boost::interprocess::file_lock lock(pathLockFile.string().c_str());
 
     // Wait maximum 10 seconds if an old wallet is still running. Avoids lockup during restart
-    if (!lock.timed_lock(boost::get_system_time() + boost::posix_time::seconds(10)))
+    if (!lock.timed_lock(boost::posix_time::microsec_clock::universal_time() + boost::posix_time::seconds(10)))
         return InitError(strprintf(_("Cannot obtain a lock on data directory %s. MasterNoder2 Core is probably already running."), strDataDir));
 
 #ifndef WIN32
@@ -1118,7 +1120,14 @@ bool AppInit2()
                 sourceFile.make_preferred();
                 backupFile.make_preferred();
                 if (boost::filesystem::exists(sourceFile)) {
-#if BOOST_VERSION >= 105800
+#if BOOST_VERSION >= 107400
+                    try {
+                        boost::filesystem::copy_file(sourceFile, backupFile, boost::filesystem::copy_options::overwrite_existing);
+                        LogPrintf("Creating backup of %s -> %s\n", sourceFile, backupFile);
+                    } catch (boost::filesystem::filesystem_error& error) {
+                        LogPrintf("Failed to create backup %s\n", error.what());
+                    }
+#elif BOOST_VERSION >= 105800
                     try {
                         boost::filesystem::copy_file(sourceFile, backupFile);
                         LogPrintf("Creating backup of %s -> %s\n", sourceFile, backupFile);
@@ -1145,7 +1154,7 @@ bool AppInit2()
                         currentFile = dir_iter->path().filename();
                         // Only add the backups for the current wallet, e.g. wallet.dat.*
                         if (dir_iter->path().stem().string() == strWalletFile) {
-                            folder_set.insert(folder_set_t::value_type(boost::filesystem::last_write_time(dir_iter->path()), *dir_iter));
+                            folder_set.insert(folder_set_t::value_type(BoostFilesystemLastWriteTime(dir_iter->path()), *dir_iter));
                         }
                     }
                 }
diff --git a/src/main.cpp b/src/main.cpp
index 174556e..7e4d39e 100755
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -47,6 +47,7 @@
 #include <boost/thread.hpp>
 #include <boost/foreach.hpp>
 #include <atomic>
+#include <deque>
 #include <queue>
 
 using namespace boost;
diff --git a/src/miner.cpp b/src/miner.cpp
index fa66dbf..6120ae2 100755
--- a/src/miner.cpp
+++ b/src/miner.cpp
@@ -30,6 +30,7 @@
 #include "zmn2chain.h"
 
 
+#include <boost/bind.hpp>
 #include <boost/thread.hpp>
 #include <boost/tuple/tuple.hpp>
 
diff --git a/src/net.cpp b/src/net.cpp
index dfc8ad5..d3858b5 100755
--- a/src/net.cpp
+++ b/src/net.cpp
@@ -13,6 +13,7 @@
 
 #include "addrman.h"
 #include "chainparams.h"
+#include "util.h"
 #include "clientversion.h"
 #include "miner.h"
 #include "obfuscation.h"
@@ -36,9 +37,15 @@
 #include <miniupnpc/upnperrors.h>
 #endif
 
+#include <boost/bind.hpp>
 #include <boost/filesystem.hpp>
 #include <boost/thread.hpp>
 
+#include <deque>
+#include <map>
+#include <set>
+#include <vector>
+
 // Dump addresses to peers.dat every 15 minutes (900s)
 #define DUMP_ADDRESSES_INTERVAL 900
 
@@ -1143,6 +1150,10 @@ void ThreadMapPort()
     struct UPNPUrls urls;
     struct IGDdatas data;
     int r;
 
-    r = UPNP_GetValidIGD(devlist, &urls, &data, lanaddr, sizeof(lanaddr));
+#if (defined(MINIUPNPC_API_VERSION) && MINIUPNPC_API_VERSION >= 18) || defined(__linux__)
+    r = UPNP_GetValidIGD(devlist, &urls, &data, lanaddr, sizeof(lanaddr), nullptr, 0);
+#else
+    r = UPNP_GetValidIGD(devlist, &urls, &data, lanaddr, sizeof(lanaddr));
+#endif
     if (r == 1) {
diff --git a/src/rpc/protocol.cpp b/src/rpc/protocol.cpp
index 69a7a1a..1a38717 100755
--- a/src/rpc/protocol.cpp
+++ b/src/rpc/protocol.cpp
@@ -74,7 +74,7 @@ static const std::string COOKIEAUTH_FILE = ".cookie";
 boost::filesystem::path GetAuthCookieFile()
 {
     boost::filesystem::path path(GetArg("-rpccookiefile", COOKIEAUTH_FILE));
-    if (!path.is_complete()) path = GetDataDir() / path;
+    if (!path.is_absolute()) path = GetDataDir() / path;
     return path;
 }
 
diff --git a/src/rpc/server.cpp b/src/rpc/server.cpp
index c53d053..928635f 100755
--- a/src/rpc/server.cpp
+++ b/src/rpc/server.cpp
@@ -17,6 +17,7 @@
 #include "utilstrencodings.h"
 
 #include <boost/bind.hpp>
+using namespace boost::placeholders;
 #include <boost/filesystem.hpp>
 #include <boost/iostreams/concepts.hpp>
 #include <boost/iostreams/stream.hpp>
diff --git a/src/script/script.h b/src/script/script.h
index c5b623b..6b483e6 100755
--- a/src/script/script.h
+++ b/src/script/script.h
@@ -377,6 +377,11 @@ protected:
 public:
     CScript() { }
     CScript(const CScript& b) : std::vector<unsigned char>(b.begin(), b.end()) { }
+    CScript& operator=(const CScript& b)
+    {
+        std::vector<unsigned char>::operator=(b);
+        return *this;
+    }
     CScript(const_iterator pbegin, const_iterator pend) : std::vector<unsigned char>(pbegin, pend) { }
     CScript(const unsigned char* pbegin, const unsigned char* pend) : std::vector<unsigned char>(pbegin, pend) { }
 
diff --git a/src/torcontrol.cpp b/src/torcontrol.cpp
index 5ae17f3..4a19f8c 100755
--- a/src/torcontrol.cpp
+++ b/src/torcontrol.cpp
@@ -18,6 +18,7 @@
 
 #include <boost/function.hpp>
 #include <boost/bind.hpp>
+using namespace boost::placeholders;
 #include <boost/signals2/signal.hpp>
 #include <boost/algorithm/string/predicate.hpp>
 #include <boost/algorithm/string/split.hpp>
diff --git a/src/util.cpp b/src/util.cpp
index 935a751..bb90fba 100755
--- a/src/util.cpp
+++ b/src/util.cpp
@@ -19,6 +19,7 @@
 #include "utilstrencodings.h"
 #include "utiltime.h"
 
+#include <chrono>
 #include <stdarg.h>
 
 #include <boost/date_time/posix_time/posix_time.hpp>
@@ -454,7 +455,7 @@ const boost::filesystem::path& GetDataDir(bool fNetSpecific)
         return path;
 
     if (mapArgs.count("-datadir")) {
-        path = fs::system_complete(mapArgs["-datadir"]);
+        path = fs::absolute(fs::path(mapArgs["-datadir"]));
         if (!fs::is_directory(path)) {
             path = "";
             return path;
@@ -476,10 +477,18 @@ void ClearDatadirCache()
     pathCachedNetSpecific = boost::filesystem::path();
 }
 
+std::time_t BoostFilesystemLastWriteTime(const boost::filesystem::path& path)
+{
+    const boost::filesystem::file_time_type ft = boost::filesystem::last_write_time(path);
+    using namespace std::chrono;
+    return system_clock::to_time_t(time_point_cast<system_clock::duration>(
+        ft - boost::filesystem::file_time_type::clock::now() + system_clock::now()));
+}
+
 boost::filesystem::path GetConfigFile()
 {
     boost::filesystem::path pathConfigFile(GetArg("-conf", "masternoder2.conf"));
-    if (!pathConfigFile.is_complete())
+    if (!pathConfigFile.is_absolute())
         pathConfigFile = GetDataDir(false) / pathConfigFile;
 
     return pathConfigFile;
@@ -488,7 +497,7 @@ boost::filesystem::path GetConfigFile()
 boost::filesystem::path GetMasternodeConfigFile()
 {
     boost::filesystem::path pathConfigFile(GetArg("-mnconf", "masternode.conf"));
-    if (!pathConfigFile.is_complete()) pathConfigFile = GetDataDir() / pathConfigFile;
+    if (!pathConfigFile.is_absolute()) pathConfigFile = GetDataDir() / pathConfigFile;
     return pathConfigFile;
 }
 
@@ -601,7 +610,7 @@ void ReadConfigFile(map<string, string>& mapSettingsRet,
 boost::filesystem::path GetPidFile()
 {
     boost::filesystem::path pathPidFile(GetArg("-pid", "masternoder2d.pid"));
-    if (!pathPidFile.is_complete()) pathPidFile = GetDataDir() / pathPidFile;
+    if (!pathPidFile.is_absolute()) pathPidFile = GetDataDir() / pathPidFile;
     return pathPidFile;
 }
 
diff --git a/src/util.h b/src/util.h
index 8548e02..6bebbc6 100755
--- a/src/util.h
+++ b/src/util.h
@@ -145,6 +145,7 @@ void ReadConfigFile(std::map<std::string, std::string>& mapSettingsRet, std::map
 boost::filesystem::path GetSpecialFolderPath(int nFolder, bool fCreate = true);
 #endif
 boost::filesystem::path GetTempPath();
+std::time_t BoostFilesystemLastWriteTime(const boost::filesystem::path& path);
 void ShrinkDebugFile();
 void runCommand(std::string strCommand);
 
diff --git a/src/validationinterface.cpp b/src/validationinterface.cpp
index bd2dfe2..04bd4f4 100755
--- a/src/validationinterface.cpp
+++ b/src/validationinterface.cpp
@@ -6,6 +6,10 @@
 
 #include "validationinterface.h"
 
+#include <boost/bind.hpp>
+
+using namespace boost::placeholders;
+
 static CMainSignals g_signals;
 
 CMainSignals& GetMainSignals()
diff --git a/src/wallet/wallet.cpp b/src/wallet/wallet.cpp
index 9275aed..dee9e00 100755
--- a/src/wallet/wallet.cpp
+++ b/src/wallet/wallet.cpp
@@ -4519,8 +4519,8 @@ void CWallet::ZPivBackupWallet()
                 //If the next file backup exists and is newer, then iterate
                 filesystem::path nextBackupPath = backupDir / strprintf("wallet-autozmn2backup-%d.dat", i + 1);
                 if (filesystem::exists(nextBackupPath)) {
-                    time_t timeThis = filesystem::last_write_time(backupPath);
-                    time_t timeNext = filesystem::last_write_time(nextBackupPath);
+                    time_t timeThis = BoostFilesystemLastWriteTime(backupPath);
+                    time_t timeNext = BoostFilesystemLastWriteTime(nextBackupPath);
                     if (timeThis > timeNext) {
                         //The next backup is created before this backup was
                         //The next backup is the correct path to use
diff --git a/src/wallet/walletdb.cpp b/src/wallet/walletdb.cpp
index 97d4473..9c4d39c 100755
--- a/src/wallet/walletdb.cpp
+++ b/src/wallet/walletdb.cpp
@@ -1022,7 +1022,7 @@ bool BackupWallet(const CWallet& wallet, const filesystem::path& strDest, bool f
                                 currentFile = dir_iter->path().filename();
                                 // Only add the backups for the current wallet, e.g. wallet.dat.*
                                 if (dir_iter->path().stem().string() == wallet.strWalletFile) {
-                                    folderSet.insert(folder_set_t::value_type(filesystem::last_write_time(dir_iter->path()), *dir_iter));
+                                    folderSet.insert(folder_set_t::value_type(BoostFilesystemLastWriteTime(dir_iter->path()), *dir_iter));
                                 }
                             }
                         }
@@ -1076,7 +1076,9 @@ bool AttemptBackupWallet(const CWallet& wallet, const filesystem::path& pathSrc,
             LogPrintf("cannot backup to wallet source file %s\n", pathDest.string());
             return false;
         }
-#if BOOST_VERSION >= 105800 /* BOOST_LIB_VERSION 1_58 */
+#if BOOST_VERSION >= 107400
+        filesystem::copy_file(pathSrc, pathDest, filesystem::copy_options::overwrite_existing);
+#elif BOOST_VERSION >= 105800 /* BOOST_LIB_VERSION 1_58 */
         filesystem::copy_file(pathSrc.c_str(), pathDest, filesystem::copy_option::overwrite_if_exists);
 #else
         std::ifstream src(pathSrc.c_str(),  std::ios::binary | std::ios::in);
